// DEPRECATED — do not use, do not import.
//
// This was an old, incomplete draft of the CV export modal, superseded
// by CVExportModal.jsx (the real, current implementation — three
// templates, Word export, escaped user content, premium watermark).
// This file is confirmed unused anywhere in the app (zero imports
// repo-wide), but it shared the exact same component name
// (`CVExportModal`) and was missing entire sections (Experience,
// Education) — a real landmine if anyone ever reconnected it by mistake,
// thinking it was the current one.
//
// The sandbox couldn't physically delete this file in this session (the
// workspace folder blocks direct deletion, and the delete-permission
// dialog needs the file owner's approval), so it's been gutted instead —
// no component, no default export, nothing left that could accidentally
// get wired back in. Safe to delete this file entirely by hand whenever
// convenient.
export {};
